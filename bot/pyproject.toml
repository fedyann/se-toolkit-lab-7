[project]
name = "bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot==20.7",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
